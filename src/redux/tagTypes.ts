export enum tagTypes {
    user = "user",
    profile = "profile",
    trip = "trip",
    tripRequest = "tripRequest",
  }
  
  export const tagTypesList = [
    tagTypes.trip,
    tagTypes.user,
    tagTypes.tripRequest,
    tagTypes.profile,
  ];